cover:
    make <command_to_generate_coverage_report>

lint:
    make <command_to_run_linter>

format:
    make <command_to_format_code>

generate:
    make <command_to_generate_files>

tidy:
    make <command_to_tidy_code>
